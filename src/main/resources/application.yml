server:
  port: 8113
  servlet:
    context-path: /firma
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: Firma-Digital

# Configuración de certificados en Base64
certificado:
  base64: {}
